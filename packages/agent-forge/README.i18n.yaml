# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each
# side as of the last confirmed-consistent state. After editing either side, bring the
# other along and re-record with: pnpm run verify-translation-pairing --write README.md
# NOTE: run inside a git repository once this package is committed; hashes pending.
README.md: pending-initial-commit
README.zh.md: pending-initial-commit

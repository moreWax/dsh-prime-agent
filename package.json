{
  "name": "dsh-prime-bridge",
  "private": true,
  "type": "module",
  "engines": { "node": "^22.19.0 || >=24.0.0" },
  "scripts": {
    "build": "tsdown --config tsdown.config.ts",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "tsdown": "^0.22.0",
    "tsx": "^4.19.0",
    "@types/node": "^22"
  }
}
